-- Миграция для добавления поля "продуктовая линейка" в таблицу курсов
-- Дата: 2025-09-03

-- Добавляем колонку product_line в таблицу courses
ALTER TABLE public.courses
ADD COLUMN IF NOT EXISTS product_line TEXT;

-- Добавляем комментарий для новой колонки
COMMENT ON COLUMN public.courses.product_line IS 'Продуктовая линейка или направление, к которому относится курс (например, ДС АВТО, КАСКО). Используется для группировки в каталоге.';

-- Также нужно добавить это поле в админ-панель, чтобы администраторы могли его задавать при создании/редактировании курса.
-- Это изменение будет внесено в admin.html и admin-handler.js.

-- SELECT 'Миграция для добавления product_line успешно подготовлена.';
